{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": [
        "@playwright/mcp",
        "--isolated",
        "--headless",
        "--browser",
        "chromium",
        "--output-dir",
        ".playwright-mcp"
      ]
    }
  }
}
